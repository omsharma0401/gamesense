# VideoDB — hackathon branch (required, not on PyPI)
git+https://github.com/Video-DB/videodb-python.git@hackathon

# Web framework
fastapi
uvicorn[standard]

# Config
python-dotenv

# LLM providers (OpenAI-compatible SDK works for both OpenRouter and Groq)
openai

# Memory — temporal knowledge graph (pulls in kuzu automatically)
graphiti-core[kuzu]

# Memory — async SQLite
aiosqlite

# HTTP client (Discord webhook, audio download, async test client)
httpx

# Validation
pydantic>=2.0

# WebSocket client for ws_listener
websockets

# Testing
pytest
pytest-asyncio
